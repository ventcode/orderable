development:
  adapter: postgresql
  database: orderable_development
  pool: 5
  timeout: 5000
  hostname: <%= ENV.fetch('POSTGRES_HOSTNAME', 'localhost') %>
  username: <%= ENV['POSTGRES_USERNAME'] %>
  password: <%= ENV['POSTGRES_PASSWORD'] %>
