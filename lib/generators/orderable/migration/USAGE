Description:
      Generates a migration, which add field to specified table 
        with unique deferrable initially deferred constraint in PSQL 
        for that field and specified scope fields
        

Example:
    bin/rails generate orderable:migration BasicModel:position name

    This will create:
        db/migrate/{timestamp}_add_unique_orderable_position_to_basic_model.rb

        
